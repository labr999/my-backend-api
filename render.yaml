services:
  - type: web
    name: my-backend-api
    runtime: docker
    plan: starter
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    autoDeploy: true
    disk:
      name: xiangqi-data
      mountPath: /app/data
      sizeGB: 1
